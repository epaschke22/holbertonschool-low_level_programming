#include <stdio.h>
#include "holberton.h"

/**
 * main - print out name of the program.
 * @argc: argument count.
 * @argv: argument vector array.
 * Return: always 0
 */
int main(int argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
